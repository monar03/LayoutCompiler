import com.sun.istack.internal.NotNull;

import java.io.OutputStream;

public class LayoutCompiler {
    @NotNull
    public Executer compile(@NotNull OutputStream outputStream) {
        return () -> {
        };
    }
}
